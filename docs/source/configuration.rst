Configuration
==============================================

You can setup a single book (`set_book_config`) or multiple books (`set_library_config`). 

Use the arguments on `set_book_config` to personalize to your needs.

.. autofunction:: __init__.set_book_config

Use the arguments on `set_library_config` to personalize to your needs.

.. autofunction:: __init__.set_library_config



